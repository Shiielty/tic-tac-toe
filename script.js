

const gameBoard = (() => {
    
    const btn = document.querySelector("button");

    const alertBtn = () => {
        btn.addEventListener("click", () => alert("Hi! Let's start this project!"))
    };

    return {alertBtn}

    // let board = [];

    // const div = document.createElement("div");

    // // create 9 box and insert to the board array
    // for (let i = 0; i < 6; i++) {
        
    
})();